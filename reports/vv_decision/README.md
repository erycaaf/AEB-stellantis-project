# Decision (TTC + FSM) V&V — Evidence artefacts

Reproducible evidence for the independent cross-validation of
[src/decision/aeb_ttc.c](../../src/decision/aeb_ttc.c) and
[src/decision/aeb_fsm.c](../../src/decision/aeb_fsm.c).
**Validator (cross):** Eryca. **Author:** Lourenço Jamba Mphili.
Aligned to the ISO 26262-6:2018 ASIL-D technique set.

The consolidated V&V report (`Consolidated_Report_VV_Decision.pdf` and its
`.tex` source) lives in the team documentation area, **outside this repo**.
Only the machine-generated evidence and the reproducibility build targets
live here.

## Reproduce everything

From the repo root, in an environment with GCC 14, gcov-14, gcovr 8.6,
cppcheck 2.13, Valgrind 3.22 and the ASan/UBSan runtimes (Ubuntu 24.04
provides all of these):

```bash
make vv-decision
```

This runs, in order:

1. `make mcdc-decision` — `-fcondition-coverage` build + gcov condition report
2. `make fault-decision` — systematic fault-injection suite (non-fatal harness)
3. `make memory-decision` — Valgrind + ASan + UBSan on all three test binaries
4. `make misra-decision` — cppcheck MISRA addon scoped to decision module

## Artefact map

| File | Produced by | Summary |
|---|---|---|
| `coverage_mcdc/run.log` | MC/DC coverage | 9 nominal + 39 complementary = 48 PASS |
| `coverage_mcdc/coverage_summary.txt` | MC/DC coverage | stmt 97.7% / branch 96.8% / MC/DC 96.6% — 3 defensively-unreachable lines + 1 MISRA-mandated NULL guard (see report for justified deviations) |
| `coverage_mcdc/coverage.xml` | MC/DC coverage | Cobertura XML (CI-consumable) |
| `coverage_mcdc/gcov_summary.txt` | MC/DC coverage | gcov-14 per-binary metrics (MCDC binary) |
| `coverage_mcdc/aeb_ttc.c.gcov` | MC/DC coverage | line-by-line annotated source (TTC) |
| `coverage_mcdc/aeb_fsm.c.gcov` | MC/DC coverage | line-by-line annotated source (FSM) |
| `fault_injection/run.log` | Fault injection | 32 assertions, 21 PASS, 11 FAIL (4 distinct vulnerabilities — see table below) |
| `memory_safety/run.log` | Memory safety | consolidated trace; all 6 tool runs `(clean)` |
| `memory_safety/valgrind_*.log` (×3) | Memory safety | per-binary Valgrind stderr — empty ⇔ 0 errors |
| `memory_safety/ubsan_*.log` (×3) | Memory safety | per-binary ASan + UBSan stderr — empty ⇔ 0 errors |
| `misra/run.log` | MISRA static analysis | cppcheck execution trace |
| `misra/cppcheck_decision.xml` | MISRA static analysis | 0 error/warning/performance/portability; 12 `style` findings in decision scope (2×8.7, 2×10.3, 5×15.5, 3×15.7); 48 advisory findings in shared headers (out of scope) |
| `functional_tests_decision.csv` | traceability | requirement → test → evidence rows for the 68 test cases (61 PASS + 7 FAIL); 7 FAIL align with the 4 robustness vulnerabilities catalogued below |

HTML MC/DC report and all test binaries are regenerated by their targets
but excluded from the commit (see `.gitignore`).

## Robustness vulnerabilities (from fault injection)

Four defects identified by the fault-injection activity; root-cause
analysis and proposed patches are in the consolidated V&V report. Per
ISO 26262-6 §5.4.8 (independence), the patches are to be applied by the
original author on a separate branch and re-validated by re-running
`make vv-decision`.

| # | Severity | Location | Evidence (this repo) |
|---|---|---|---|
| 1 | HIGH (robustness) | `src/decision/aeb_ttc.c:40-54` | `fault_injection/run.log` (A4, A5, B2) |
| 2 | CRITICAL (SEU) | `src/decision/aeb_fsm.c:175` | `fault_injection/run.log` (C3) |
| 3 | HIGH (robustness) | `src/decision/aeb_fsm.c:165` | `fault_injection/run.log` (A6) |
| 4 | CRITICAL (SEU) | `src/decision/aeb_fsm.c:230, :328` | `fault_injection/run.log` (C1, C2) |

The MC/DC coverage activity additionally flagged 3 defensively unreachable
lines (dead code per MISRA C:2012 Rule 2.1) — removal proposal in the
consolidated V&V report.

## Functional Test Excel

`functional_tests_decision.csv` is the canonical data for the Decision
tab of the shared Functional Test Excel. Schema follows the project-standard
`Requirement | Test function | Input | Expected | P/F | Evidence`, with
one row per test function across the three suites (nominal, complementary
MC/DC, and fault injection).

Coverage: FR-DEC-001..011; FR-FSM-001..006; FR-ALR-003; FR-BRK-005,
FR-BRK-006; FR-PER-004, FR-PER-005, FR-PER-006; FR-UDS-004;
and the NFR-SAF-ROB robustness family.

The 7 rows marked `F` correspond to fault-injection assertions that
currently reveal the four vulnerabilities listed in the section below;
they flip to `P` once the original author applies the proposed patches.
